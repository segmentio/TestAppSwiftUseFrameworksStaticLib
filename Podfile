# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestAppSwift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Analytics'
  pod 'Segment-GoogleAnalytics/StaticLibWorkaround', :git => 'https://github.com/segment-integrations/analytics-ios-integration-google-analytics.git', :branch => 'static-lib-workaround'
  pod 'GoogleAnalytics'
  pod 'GoogleIDFASupport'

  # Pods for TestAppSwift

end
